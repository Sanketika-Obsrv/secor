apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: {{ template "secor.fullname" . }}
  labels:
    app: {{ template "secor.fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  serviceName: {{ template "secor.fullname" . }}
  updateStrategy:
    type: RollingUpdate
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ template "secor.fullname" . }}
        release: {{ .Release.Name }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9102"
        prometheus.io/path: "/metrics"
    spec:
      securityContext:
{{ toYaml .Values.podSecurityContext | indent 8 }}
      containers:
        - name: secor
          image: {{ .Values.image.prefix }}{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
{{ toYaml .Values.containerSecurityContext | indent 12 }}

          env:
            - name: "CONFIG_FILE"
              value: "{{ .Values.secor.partition.configFile }}"
            - name: "LOG4J_CONFIGURATION"
              value: "{{ .Values.secor.partition.logConfigFile }}"
            - name: "GOOGLE_APPLICATION_CREDENTIALS"
              value: "/var/run/secret/cloud.google.com/service-account.json"
            - name: "ZOOKEEPER_QUORUM"
              value: "{{ .Values.secor.zookeeper.quorum }}"
            - name: "ZOOKEEPER_PATH"
              value: "{{ .Values.secor.zookeeper.path }}"
            - name: "JVM_MEMORY"
              value: "{{ .Values.secor.partition.jvm.memory }}"

          volumeMounts:
           - mountPath: /opt/secor/config
             name: config
           - name: "service-account"
             mountPath: "/var/run/secret/cloud.google.com"
           - name: "local-var"
             mountPath: "/mnt/secor_data/message_logs/partition"
           - name: "tmp"
             mountPath: "/tmp"
          resources:
{{ toYaml .Values.secor.partition.resources | indent 12 }}

        - name: monitor
          image: {{ .Values.image.prefix }}{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
{{ toYaml .Values.containerSecurityContext | indent 12 }}
          env:
            - name: "CONFIG_FILE"
              value: "{{ .Values.secor.partition.configFile }}"
            - name: "LOG4J_CONFIGURATION"
              value: "{{ .Values.secor.partition.monitor.logConfigFile }}"
            - name: "SECOR_MAIN_CLASS"
              value: "com.pinterest.secor.main.ProgressMonitorMain"
            - name: "GOOGLE_APPLICATION_CREDENTIALS"
              value: "/var/run/secret/cloud.google.com/service-account.json"
            - name: "ZOOKEEPER_QUORUM"
              value: "{{ .Values.secor.zookeeper.quorum }}"
            - name: "ZOOKEEPER_PATH"
              value: "{{ .Values.secor.zookeeper.path }}"

          volumeMounts:
            - mountPath: /opt/secor/config
              name: config
            - name: "service-account"
              mountPath: "/var/run/secret/cloud.google.com"
            - name: "local-var"
              mountPath: "/mnt/secor_data/message_logs/partition"
            - name: "tmp"
              mountPath: "/tmp"

        - name: statsd
          image: {{ .Values.exporter.image.repository }}:{{ .Values.exporter.image.tag }}
          imagePullPolicy: {{ .Values.exporter.image.pullPolicy }}
          securityContext:
{{ toYaml .Values.containerSecurityContext | indent 12 }}
          ports:
            - containerPort: 9102
            - containerPort: 9125
              protocol: UDP

      volumes:
       - configMap:
           name: secor-config
         name: config

       - name: "service-account"
         secret:
           secretName: "secor-service-account"

       # Scratch space, required because the root filesystem is read-only.
       - name: "tmp"
         emptyDir: {}

  volumeClaimTemplates:
    - metadata:
        name: local-var
        labels:
          app: {{ template "secor.fullname" . }}
        annotations:
          volume.beta.kubernetes.io/storage-class: "{{ .Values.secor.partition.storage.class }}"
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: "{{ .Values.secor.partition.storage.size }}"
